# Dependencies

Every dependency added to this repo is recorded here: what it does, what
it replaces, what it costs (install size / maintenance surface), and why
it isn't stdlib or pandas-only. Pin exact versions in `pyproject.toml`;
this file explains the *why*, `pyproject.toml` is the *what*.

| Name | Version | Purpose | Replaces | Why not stdlib |
|---|---|---|---|---|
| pydantic | 2.9.2 | Typed, validated settings/models for `RiskLimits` and `ResearchPolicy`. Frozen models give us the "attribute assignment raises" guarantee Law 4 needs for free. | Hand-rolled `dataclass` + manual validation | stdlib `dataclasses` don't validate on construction or reject attribute assignment on frozen instances the same way (no field-level validators, no env parsing) |
| pydantic-settings | 2.5.2 | Environment-variable loading for `RiskLimits` with fail-fast-on-missing semantics. | `os.environ` + manual parsing | Manual env parsing silently defaults instead of raising; we need import-time hard failure |
| sqlalchemy | 2.0.35 | Async ORM + Core for `core/db.py`, models for experiments/results/decisions/policy_versions/id_counters. | Raw `asyncpg` queries | Need declarative models Alembic can autogenerate against, and a session API later modules build on |
| alembic | 1.13.3 | Schema migrations, including the append-only trigger DDL. | Hand-run SQL scripts | Need ordered, reversible, version-controlled schema history from day one — Law 6 depends on the trigger existing in every environment identically |
| asyncpg | 0.31.0 | Async Postgres driver for the SQLAlchemy async engine at runtime. | `psycopg` async mode | asyncpg is the SQLAlchemy-recommended, fastest async Postgres driver. Pinned to 0.31.0 rather than 0.29.0 because 0.29.0 ships no prebuilt wheel for the CPython 3.13 interpreter available in this environment and fails to build from source against the 3.13 C API; 0.31.0 ships a cp313 wheel. |
| psycopg[binary] | 3.2.3 | Sync Postgres driver for Alembic (migrations run sync) and the sync test connection used by `test_history_append_only`. | asyncpg in sync mode (not supported) | Alembic's autogenerate and offline/online migration runner assume a sync driver |
| pyyaml | 6.0.2 | Parse `config/research_policy.yaml` for `ResearchPolicy`. | Hand-written YAML subset parser | YAML is a full spec (anchors, multi-doc); stdlib has no YAML parser at all |
| ruff | 0.6.9 (dev) | Lint + format, single fast binary. | flake8 + black + isort | One tool, one config block, far faster on this codebase's eventual size |
| mypy | 1.11.2 (dev) | Static typing, strict on `core/` and `validation/` per CLAUDE.md. | No static typing | CLAUDE.md mandates strict typing on the two law-adjacent packages |
| pytest | 8.3.3 (dev) | Test runner, including `tests/laws/`. | stdlib `unittest` | Fixtures, parametrize, and `xfail(strict=True)` are load-bearing for this repo's law-scaffolding pattern |
| pytest-asyncio | 0.24.0 (dev) | Run `async def test_...` against the async engine/session. | Manual `asyncio.run()` wrapping in every test | Native async test support, `asyncio_mode = "auto"` keeps test code free of boilerplate |
| pre-commit | 3.8.0 (dev) | Run ruff + mypy on every commit locally, matching CI. | CI-only enforcement | Catches law-relevant typing/lint breaks before they're pushed |
| types-PyYAML | 6.0.12.20240917 (dev) | Type stubs for `pyyaml`, so `yaml.safe_load` is typed rather than `Any` under mypy strict on `core/`. | `# type: ignore` on every YAML call site | pyyaml ships no inline types (no `py.typed`); without stubs mypy strict cannot check `core/config.py`'s policy loader at all |
| hatchling | >=1.25 (build) | PEP 517 build backend for `pip install -e .`. | setuptools | setuptools needs `setup.py`/`setup.cfg` plus its own config surface; hatchling reads `pyproject.toml` only. **The one dependency not exactly pinned** — it is a build-time backend that never enters the runtime environment or a backtest, so it is outside the reproducibility boundary that "pin everything" protects. |
| polars | 1.44.1 | Feature math on OHLCV data; `PointInTimeFrame`'s lazy-frame `select()` is what makes `event_time` structurally droppable from feature code's view. | pandas for this layer | Lazy evaluation + explicit column selection make "this column doesn't exist for you" a real schema property, not a convention; pandas has no lazy frame. pandas stays at DB/ORM boundaries only, per CLAUDE.md's stack. |
| ccxt | 4.5.77 | Exchange data ingestion — Binance spot OHLCV. | Hand-rolled REST client + retry/rate-limit logic | Handles rate limiting, pagination, and exchange quirks across a uniform interface; we use only its Binance spot adapter. |
| cpz-quant | 1.1.0 | PROMPTS.md PROMPT 5's validation layer: Probability of Backtest Overfitting (`certification.overfitting.probability_of_backtest_overfitting`, CSCV), Sharpe/Sortino/Calmar/max-drawdown/tail-ratio (`certification.analytics.compute_risk_analytics`), and purge+embargo cross-validation splitters (`portfolio.model_selection.CombinatorialPurgedCV`/`WalkForward`). | Hand-rolled PBO/Sharpe/CV splitting | CLAUDE.md is explicit: do not reimplement PBO or Sharpe from scratch, use cpz-quant. **Read its actual source before wiring it in** (github.com/CPZ-Lab/cpz-quant, Apache-2.0, `certification/grade.py`, `certification/overfitting.py`, `certification/analytics.py`, `portfolio/model_selection.py`): it genuinely computes PBO and risk analytics, but `CertRigor.deflated_sharpe`/`probabilistic_sharpe` are *input* fields on a grading dataclass — the actual DSR/PSR computation lives only in the proprietary `cpz-ai` SDK this package does not ship. `validation/multiple_testing.py` therefore hand-rolls DSR/PSR from the published closed form (Bailey & López de Prado 2014, "The Deflated Sharpe Ratio") rather than pretending cpz-quant provides it — same category of exception as the null suite's `z=1.96` constant: citable public-domain math, not an invented threshold. Pure-Python (`py3-none-any` wheel), no compiled-extension risk. |
| scipy | 1.16.3 | cpz-quant's own declared dependency (`numpy>=1.26`, `scipy>=1.11`) — not imported directly by this repo's code. | — | Not an independent choice; required transitively by cpz-quant. Genuinely new weight in the production container (~36MB manylinux wheel) — recorded here rather than left as unexplained transitive size, per this file's own rule. Verified a real `cp311-manylinux2014_x86_64` wheel exists (matches the Dockerfile's `python:3.11-slim`) before pinning, not assumed. |
| fastapi | 0.115.0 | The `/world`, `/health`, `/scoreboard`, `/buildings` API and the WebSocket that pushes world-state deltas (PROMPT 1). | Hand-rolled ASGI routing + manual request validation | Pydantic-native request/response models (reuses `world/entities.py`'s existing `BaseModel`s directly), dependency injection for the DB session, and automatic OpenAPI docs for free. |
| uvicorn | 0.30.6 | ASGI server that actually runs the FastAPI app (`prometheus.main:app`), including in the production Dockerfile's `CMD`. | `python -m http.server` (not ASGI-capable) | FastAPI is an ASGI framework; it needs an ASGI server, and uvicorn is the one the FastAPI docs themselves recommend and the one this codebase's Dockerfile assumes. |
| httpx | 0.27.2 | Async HTTP client for arXiv metadata fetching, PDF downloads, and GROBID calls in `research/llm/ingestion.py`. Also used by FastAPI's `TestClient` for in-process endpoint testing. | `requests` + a real running server in tests | `TestClient` is built on `httpx`; without it, testing the API layer means actually binding a port per test run, which is slower and flakier. **Moved from dev-only to main dependencies in PROMPT 9** — `ingestion.py` now imports it at runtime for arXiv/PDF/GROBID HTTP calls alongside its existing TestClient-only use. |
| pypdf | 6.19.0 | Full-text extraction from downloaded arXiv PDFs (`research/llm/ingestion.py`) -- archival `full_text` storage and the fallback path when GROBID is unreachable. | Hand-rolled PDF parsing or external service dependency | stdlib has no PDF codec at all. Pure-Python, permissive (BSD-3-Clause) license, no compiled-extension risk -- simpler and lighter than PyMuPDF (AGPL-licensed) for this project's needs, which don't require PyMuPDF's layout-precision. |
| anthropic | 1.6.0 | PROMPT 9's LLM hypothesis generation (research/llm/hypothesis.py). | no prior LLM integration existed | stdlib has no HTTP client suited to a typed streaming API surface, and hand-rolling the Messages API's request/response shapes would just be a worse reimplementation of the official SDK. |
| next | 14.2.35 (frontend) | Next.js App Router — statically exports the world view (`output: 'export'`) so it can be served by FastAPI's `StaticFiles` from the same container, per PROMPTS.md PROMPT 1 Part D. | Hand-rolled bundler/router config | App Router + static export is the documented, supported path to "one container, no separate frontend host." Pinned to 14.2.35, not 14.2.5, because 14.2.5 carries a critical CVE (cache poisoning); the remaining "high" advisories on 14.x are all in Server Actions/Middleware/RSC caching/Image Optimization — none of which this app uses (no middleware, no server actions, `images: { unoptimized: true }`), so a Next 16 major bump wasn't forced under deploy time pressure. Revisit before adding any dynamic-server feature. |
| react / react-dom | 18.3.1 (frontend) | Required peer of Next.js 14's App Router. | — | Not a choice — Next.js 14 requires React 18. |
| pixi.js | 8.1.6 (frontend) | Renders the isometric world (buildings, later agents/heroes) — batched sprite rendering for the 300-agent/60fps budget in PROMPTS.md. | Canvas2D by hand, or three.js | PROMPTS.md is explicit: PixiJS over three.js, because this is 2.5D sprite work and Pixi's batched renderer handles hundreds of animated sprites at 60fps in a way a 3D scene graph won't. Used via its plain imperative API directly (`@pixi/react`, which the adopted code had pinned at v7.1.2 against pixi.js v8 — a real version mismatch, the v7 React wrapper targets pixi.js v7's API — was dropped rather than fixed, since nothing in the code actually used its declarative components anyway). |
| recharts | 2.12.7 (frontend) | Truth View's charts (equity curves, once real backtest data exists). | D3 by hand | PROMPTS.md specifies Recharts for Truth View. Not yet wired to real chart data — Truth View currently renders scoreboard/law numbers as plain text; charts land once `backtest/benchmark.py` (a later Prompt 2 task) produces an equity curve to plot. |
| @tanstack/react-query | 5.102.8 (frontend) | The one data adapter module (`frontend/src/data/queries.ts`) polling `/world/state`, `/buildings/`, `/scoreboard/` — caching/dedup so `WorldView.tsx` and the `/entities` debug route never call `fetch` directly. | Hand-rolled `useEffect`+`setInterval` (the prior pattern) | WORLD_CONSTITUTION.md's W0.3 requires exactly one data adapter module; React Query gives real cache/dedup/refetch semantics for free instead of re-implementing them. |
| tailwindcss / @tailwindcss/postcss / postcss | 4.3.3 / 4.3.3 / 8.5.28 (frontend, postcss+tailwindcss dev-only) | Required by shadcn/ui, which needs Tailwind's utility classes — used for the new truth-layer components (`TruthDrawer`, `SearchPalette`, `BenchmarkStrip`). The world canvas itself stays untouched (inline styles, unaffected). | Plain CSS / CSS Modules | shadcn/ui's component registry is Tailwind-only; hand-authoring the same primitives (drawer, command palette, tooltip, focus management, a11y) would be strictly more code for a worse result. |
| shadcn (CLI) | 4.21.0 (frontend, dev) | Scaffolds/updates the owned component source under `src/components/ui/` (`sheet`, `command`, `badge`, `tooltip`, `button`, `dialog`, `input(-group)`, `textarea`) — a code generator, never imported at runtime, hence dev-only. | Hand-building each Radix/Base-UI wrapper | The generated files are committed and owned like any other source — the CLI itself is only ever invoked locally to add or update a component. |
| @base-ui/react | 1.8.0 (frontend) | The unstyled interaction primitives (`Dialog`→Sheet, focus trap, Escape-to-close) shadcn's `base-nova` preset generates against. | Radix UI | Not a choice made independently — it's what the shadcn preset this project initialized with (`base-nova`) targets; switching presets later would mean regenerating every `ui/` component. |
| cmdk | 1.1.1 (frontend) | The Cmd/Ctrl-K command palette's list/filter/keyboard-nav engine (`SearchPalette.tsx`, WORLD_CONSTITUTION.md's W1.3). | Hand-rolled fuzzy-filter + keyboard handling | cmdk is the de-facto standard this exact interaction pattern is built on; shadcn's `command` component is a styled wrapper around it, not a reason on its own. |
| lucide-react, class-variance-authority, tw-animate-css, cn | 1.46.0 / 0.7.1 / 1.4.0 / 0.3.0 (frontend) | Icons, variant-class composition, animation utility classes, and the `cn()` classname merge helper — all pulled in as shadcn/ui's own dependencies for the components added above. | — | Not independent choices; every shadcn/ui installation carries these four. Declared here rather than left as unexplained transitive noise. |
| typescript | 5.5.3 (frontend, dev) | Type-checking for the frontend, run as part of `next build`. | Plain JS | Matches CLAUDE.md's "strict typing where the language supports it." |
| vitest | 2.1.9 (frontend, dev) | Unit-tests the pure-math `iso/` (grid<->screen, depth sorting) and `sprites/registry.ts` (footprint classification) modules introduced by the isometric renderer rebuild. | Node's built-in `node:test` runner | `node:test` has no native TypeScript transform and fights this project's `tsconfig.json` (`moduleResolution: "bundler"`, `module: "esnext"`) without extra tooling; Vitest is Vite-based and resolves both natively. Pinned to 2.1.9 rather than the current 5.x line because 5.x's `vite` peer requires `@types/node` ^22, which would drag the whole frontend off the Node 20 toolchain the Dockerfile pins (`node:20-slim`) for a dev-only test runner; 2.1.9 is compatible with `@types/node` 20.x. This pin carries one moderate, dev-only transitive advisory in its bundled `esbuild`/`vite` (GHSA-67mh-4wv8-2f99, exposure is `vite dev`'s dev server accepting cross-origin requests) that does not apply here since only `vitest run` (one-shot, no dev server) is ever invoked, in CI and locally — never in the production container. Dev-only — never imported by runtime code, mirroring the `httpx` precedent. |
| pillow | 12.3.0 (art) | Every image operation in the `tools/art/` concept-art pipeline: decoding the nine `Mockups/BATCH 0*.jpg` sheets, LANCZOS rescaling to canonical footprint widths, `ImageOps.posterize`, the median-cut 64-colour palette derivation that writes `frontend/src/sprites/sprite_palette.json`, and writing the packed atlas PNGs to `frontend/public/sprites/`. | The retired `tools/render_vox.py`/`pack_atlas.py`/`verify_atlas.py`, which imported PIL without ever declaring it | stdlib has no image codec at all — no JPEG decoder, no PNG encoder, no resampling, no colour quantiser. Art-build-only: never imported by `prometheus/` runtime code, so it lives in the `art` extra rather than main `dependencies` and never enters the production container. Declaring it here fixes the pre-existing gap where the old tools used PIL undeclared. |
| numpy | 2.4.6 | Whole-array pixel work in `tools/art/` (checkerboard-period autocorrelation, alpha-mask classification, 3x3 erosion/re-dilation edge repair, flood-fill connected-component sprite slicing, anchor centroid math) **and**, as of PROMPT 5, cpz-quant's required array backend for `validation/`. | Nested Python loops over `Image.getdata()` | Per-pixel `tools/art/` operations over ~9.7M source pixels are two to three orders of magnitude slower in pure Python. **Moved from the `art` extra into main `dependencies` in PROMPT 5** — cpz-quant (`numpy>=1.26`) now needs it in the production container regardless of the art pipeline, so keeping it art-only would have meant declaring it twice. Pinned to 2.4.6, not 2.5.2: 2.5.x requires `python>=3.12` and breaks this repo's `py3.11` floor; 2.4.6 also satisfies scipy 1.16.3's `numpy<2.6,>=1.25.2` bound. |

**External art-processing tools evaluated and not adopted (2026-09-09):** before writing `tools/art/`, three repos named in the prompt were cloned and inspected — `0xheycat/isometric-game-skills` (already used above for grid-math attribution; it's a documentation-only Claude Code skill package, no image-processing code exists in it to adopt), `aldegad/sprite-gen` (Apache-2.0; a real Pillow+NumPy library, but built around an AI-generation workflow — chroma-key magenta/green matting on a known fixed grid, plus a curation webapp — none of which fits hand-painted, irregular, checkerboard-transparent JPEG sheets), and `capthvnsen/pixel-forge` (MIT; a real library+CLI, but a YAML-spec-driven procedural pixel-art toolkit whose render target is Godot 4, whose directional-sheet importer assumes a fixed compass grid on already-clean PNG alpha, and whose palette module resolves pre-named colours rather than deriving a palette from raw pixels via clustering). Each repo's relevant module is tightly coupled to a different contract (chroma-key + grid, or YAML asset spec + Godot export) that would need to be dragged in whole to reuse a handful of generic ideas — checkerboard/JPEG alpha recovery, BFS connected-components, pixel-centroid anchors, median-cut palette clustering, and atlas packing against this repo's own `AtlasSpec` — each of which is 20-80 lines to write directly against exactly this repo's schema. Built in-house in `tools/art/`; nothing from any of the three was imported or vendored.

**PixelLab character import (2026-09-14):** wiring the 38 PixelLab character
sprites (`art/characters/`) into the renderer is the same "reuse or build
in-house" question the 2026-09-09 evaluation above already answered for this
codebase's art pipeline -- no new dependency, no new repo evaluation needed.
`tools/art/import_pixellab.py` extends the existing `tools/art/` pipeline
(reuses `common.anchor_character`, `compute_anchors.py`, `pack_atlas.py`'s
packer) rather than duplicating it; `frontend/src/sprites/direction.ts` is a
~15-line pure function, not something worth a dependency for.

**GROBID for paper section extraction, VibeQuant does not exist as claimed (2026-09-18):** PROMPTS.md PROMPT 9 names "VibeQuant" as a reference for arXiv/paper extraction ideas. Web search found several small, unrelated repos sharing that name (a `yfinance`-wrapping finance analysis library, a couple of near-empty forks) -- none has any paper/PDF extraction functionality, the same "named repo doesn't fit reality" outcome already documented for Qubx (PROMPT 8). Instead, `kermitt2/grobid` (5.1k GitHub stars, Apache 2.0) was evaluated and adopted: a real, production-proven ML-based structured-extraction tool for exactly this job (title/abstract/section segmentation from scientific PDFs), used in production by ResearchGate, CERN, and Mendeley among others. It is a Java service, not a Python library -- deployed as its own Railway service with scale-to-zero enabled (configured directly in Railway's dashboard; no committed config in this repo, same posture as every other Railway-console-only setting) rather than an always-on third service, keeping CLAUDE.md's "two always-on services + one worker" cost target intact. `research/llm/ingestion.py` calls it over Railway's private network (`GROBID_URL` env var) and falls back to `pypdf`-only extraction if it's unreachable -- a soft failure, never a hard one.

**Isometric math attribution:** PROMPTS.md asks that the isometric grid math be ported from `github.com/0xheycat/isometric-game-skills` (MIT-licensed; algorithms only, not its Canvas2D/Godot rendering code or ComfyUI/SDXL art pipeline). The 2:1 screen&lt;-&gt;grid projection formula and depth sorting are now implemented (`frontend/src/iso/projection.ts`'s `gridToScreen`/`screenToGrid`/`depthOf`), matching that repo's `isometric-grid-math` and `depth-sorting-occlusion` skill references (`screenX=(col-row)*(W/2)`, `screenY=(col+row)*(H/2)`; depth keyed on a multi-tile object's far-most occupied tile, not its origin, so large buildings sort correctly against smaller neighbours). The hand-authored city layout (`prometheus/world/construction.py`'s `BUILDING_LOCATIONS`, validated by `tests/test_building_layout.py`) replaces the earlier arrangement, which had real overlapping footprints (the Monument literally overlapped both the Forge and the Vault). A* pathfinding and autotiling from that toolkit remain deferred: `agents` and `districts` are still always `[]` — `prometheus/world/projection.py` hardcodes this with an explicit comment that a fabricated agent would be the world lying about work it is not doing, and there is still no jobs table or scheduler anywhere in the backend (PROMPT 4). The builder sprites the renderer now shows are static idle decoration keyed off each building's real `construction_phase` (PROMPTS.md's actual Prompt-1 acceptance bar), not the pathfinding-driven migration system PROMPTS.md describes for later — that still needs Prompt 4's real per-agent backend state before it can be built without inventing data. Revisit pathfinding when Prompt 4 or later actually produces a moving agent with real `from_location`/`to_location`/`progress`.

**Qubx evaluated and not adopted (2026-09-17):** PROMPTS.md PROMPT 8 asks
that `github.com/xLydianSoftware/Qubx` be evaluated as the paper-trading
execution layer before building our own. Checked via `gh repo view
xLydianSoftware/Qubx`: GPLv3-licensed ("Framework for quantitative
strategies development, backtesting and live execution"), primary
language Jupyter Notebook, 69 stars. Two disqualifying findings, not a
technical feature comparison: (1) GPLv3 linked into this codebase risks
obligating the whole combined work to GPLv3 if this repo is ever
distributed, an unforced risk when `ccxt` (already an MIT dependency,
already used in `data/ingestion.py`) covers everything a testnet broker
adapter needs directly; (2) a notebook-first repo is a poor fit for the
bounded, async, Postgres-`SKIP LOCKED`-queue-driven worker this codebase
already runs (`worker.py`, `experiments/queue.py`) -- there is no clean
headless entrypoint to integrate against without adopting its whole
framework shape. `paper/broker.py` is a ~100-line `ccxt.binance()`
wrapper instead. PROMPTS.md explicitly permits concluding "our adapter is
simpler" -- this is that conclusion.
